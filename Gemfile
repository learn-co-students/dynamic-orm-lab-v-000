# A sample Gemfile
source "https://rubygems.org"

gem 'pry'
gem 'sqlite3'

#added the below due to: require': cannot load such file -- sqlite3 (LoadError)
gem 'rspec'

#added the below due to: cannot load such file -- active_support/inflector (LoadError)
gem 'activesupport-inflector', '~> 0.1.0'
gem 'i18n', '~> 0.7.0'